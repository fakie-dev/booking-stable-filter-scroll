{
  "name": "booking-stable-filter-scroll",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26 <27"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/sync-build.mjs",
    "check": "node --check src/main.js && node --check booking-stable-filter-scroll.user.js && node scripts/validate.mjs",
    "test": "node --test",
    "test:browser": "npm run build && node scripts/browser-smoke.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-monkey": "^8.1.1"
  }
}
